import React from 'react';

const Portfolio: React.FC = () => {
    return (
        <section id="portfolio">
            <h2>Portfolio</h2>
            <p>Showcase of previous work or projects.</p>
            {/* Add portfolio items here */}
        </section>
    );
};

export default Portfolio;